meta:
  environment: python
  toolchain:
    name: pip
    version: "3.12"
build:
  requirementsPath: requirements.txt
run:
  scriptName: parser_api.py
  persistenceMount: /data
  containerPort: "80"
